package it.polimi.ingsw.controller.match;

/**
 * List of the different phases of the match
 */
public enum MatchPhase {
    LOBBY, PRE_MATCH, LEADER_SELECTION, TURN_START, MARKETPLACE, TURN_END, GAME_OVER
}
